Realize a INTEGRAÇÃO COM O BACKEND (n8n) e POLISH FINAL do sistema.

PARTE 1 - CONFIGURAÇÃO DE API:

Crie um serviço de API centralizado:

Arquivo `src/services/api.ts`:
```typescript
const API_BASE_URL = 'https://n8n-n8n-otica-suellenn.gycquy.easypanel.host';

const api = {
  // Conversas
  async getConversas() {
    // Buscar via endpoint n8n se necessário
    // Por ora retornar mock
  },
  
  async getConversa(id: string) {
    // Buscar conversa específica
  },
  
  async enviarMensagem(conversaId: string, mensagem: string, tipo: string) {
    return fetch(`${API_BASE_URL}/webhook/whatsapp/send`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        numero: conversaId, // ajustar conforme necessário
        conversaId,
        tipo,
        mensagem
      })
    });
  },
  
  async transferirConversa(conversaId: string, novoAtendenteId: string, motivo: string) {
    return fetch(`${API_BASE_URL}/webhook/whatsapp/transferir`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        conversaId,
        novoAtendenteId,
        motivo
      })
    });
  },
  
  // Botões
  async criarBotao(botao: any) {
    return fetch(`${API_BASE_URL}/webhook/botao/criar`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(botao)
    });
  },
  
  async enviarBotao(botaoId: string, conversaId: string, numero: string) {
    return fetch(`${API_BASE_URL}/webhook/botao/enviar`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({
        botaoId,
        conversaId,
        numero
      })
    });
  },
  
  // Horário
  async checkHorario() {
    return fetch(`${API_BASE_URL}/webhook/whatsapp/check-horario`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({})
    });
  }
};

export default api;
```

Integre este serviço nas telas:
- Conversas: enviar mensagens reais
- Botões: criar e enviar botões reais
- Configurações: verificar horário

---

PARTE 2 - WEBSOCKET/POLLING PARA TEMPO REAL:

Adicione polling para atualizar conversas a cada 10 segundos:
```typescript
useEffect(() => {
  const interval = setInterval(() => {
    // Atualizar lista de conversas
    refreshConversas();
  }, 10000);
  
  return () => clearInterval(interval);
}, []);
```

Notificações desktop quando nova mensagem:
```typescript
if ('Notification' in window && Notification.permission === 'granted') {
  new Notification('Nova mensagem!', {
    body: `De ${cliente}: ${mensagem}`,
    icon: '/logo.png'
  });
}
```

---

PARTE 3 - MELHORIAS E POLISH:

ANIMAÇÕES:
- Adicione Framer Motion para:
  * Transições de página (fade in/out)
  * Modais (slide up)
  * Lista de conversas (stagger children)
  * Botões (scale on hover)
  * Toasts (slide in from top-right)
```typescript
import { motion } from 'framer-motion';

<motion.div
  initial={{ opacity: 0, y: 20 }}
  animate={{ opacity: 1, y: 0 }}
  exit={{ opacity: 0, y: -20 }}
  transition={{ duration: 0.3 }}
>
  {/* conteúdo */}
</motion.div>
```

ATALHOS DE TECLADO:
- Cmd/Ctrl + K: Busca global
- Cmd/Ctrl + /: Focar no input de mensagem
- Esc: Fechar modais
- ↑↓: Navegar na lista de conversas
- Enter: Abrir conversa selecionada
- Cmd/Ctrl + Enter: Enviar mensagem
- Cmd/Ctrl + N: Nova conversa
- Cmd/Ctrl + B: Novo botão

Implementar com:
```typescript
import { useHotkeys } from 'react-hotkeys-hook';

useHotkeys('cmd+k, ctrl+k', (e) => {
  e.preventDefault();
  openSearch();
});
```

ESTADOS DE LOADING GLOBAIS:
- Skeleton loaders em todas as listas
- Shimmer effect nos cards
- Progress bar no topo da página durante navegação
- Spinners em botões durante ações

EMPTY STATES MELHORADOS:
- Ilustrações SVG customizadas
- Mensagens amigáveis e acionáveis
- CTAs claros
- Exemplos de uso

MICRO-INTERAÇÕES:
- Ripple effect em botões
- Checkbox com animação de check
- Toggle switch com slide suave
- Input com underline animado no focus
- Avatar com status badge pulsante (online)

FEEDBACK VISUAL:
- Toast notifications estilizadas
- Confirmações antes de ações destrutivas
- Success states com animação de check
- Error states com shake animation
- Warning states com pulse

OTIMIZAÇÕES:
- Lazy loading de imagens
- Code splitting por rota
- Memoização de componentes pesados
- Debounce em inputs de busca
- Virtual scrolling em listas longas

---

PARTE 4 - TESTES E VALIDAÇÃO:

Teste cada funcionalidade:
- [ ] Login e autenticação
- [ ] Navegação entre todas as telas
- [ ] Envio de mensagem (chamada real à API)
- [ ] Criação de botão (chamada real à API)
- [ ] Transferência de conversa
- [ ] Filtros e ordenação
- [ ] Modais e drawers
- [ ] Responsividade em mobile
- [ ] Atalhos de teclado
- [ ] Notificações
- [ ] Dark mode (se implementado)
- [ ] Estados de loading
- [ ] Estados de erro
- [ ] Empty states

---

PARTE 5 - DOCUMENTAÇÃO:

Crie arquivo README.md no projeto:
```markdown
# Ótica Suellenn - Painel de Atendimento WhatsApp

## 🚀 Funcionalidades
- Atendimento WhatsApp em tempo real
- Botões de resposta rápida customizáveis
- Gestão completa de clientes e produtos
- Relatórios e analytics
- Sistema de filas automático
- Multi-atendentes

## 🛠 Stack Tecnológica
- React 18 + TypeScript
- Tailwind CSS
- Shadcn/ui
- Recharts
- Framer Motion
- React Router

## 🔗 Backend
- n8n workflows
- Supabase database
- WhatsApp Business API

## ⚙️ Configuração
1. Clone o repositório
2. `npm install`
3. Configure `.env` com URL do n8n
4. `npm run dev`

## 📱 Integração WhatsApp
Configure no painel de Configurações > WhatsApp

## 🎨 Personalização
Ajuste cores e tema em `src/config/theme.ts`

## 📄 Licença
Proprietary - Ótica Suellenn
```

---

PARTE 6 - TOQUES FINAIS:

FAVICON E LOGO:
- Adicione favicon personalizado
- Logo em SVG responsivo
- Loading screen com logo animado

MENSAGENS DE ERRO AMIGÁVEIS:
Substituir erros técnicos por mensagens claras:
- "Ops! Algo deu errado. Tente novamente."
- "Não foi possível conectar. Verifique sua internet."
- "Esta ação requer permissão de administrador."

TUTORIAIS IN-APP:
- Tooltips em features complexas
- Tours guiados para novos usuários
- Video embeds tutoriais (opcional)

AJUDA E SUPORTE:
- Botão flutuante de ajuda (ícone Help)
- Link para documentação
- Chat de suporte (widget)
- FAQs contextuais

ACESSIBILIDADE FINAL:
- Verificar contraste de todas as cores
- Testar navegação apenas por teclado
- Adicionar skip links
- ARIA labels completos
- Focus traps em modais

---

Por favor, implemente todas essas integrações e melhorias. Foque especialmente em:
1. Conectar as funcionalidades principais com a API real do n8n
2. Adicionar animações suaves e profissionais
3. Garantir responsividade perfeita
4. Implementar atalhos de teclado
5. Polish visual em todos os detalhes

O resultado deve ser um sistema completo, profissional, e 100% funcional!